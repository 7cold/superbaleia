import 'package:flutter/material.dart';
import 'fonts.dart';

TextStyle textHeavy(double size, int color) => TextStyle(
      fontFamily: fontHeavy,
      color: Color(color),
      fontSize: size,
    );

TextStyle textBold(double size, int color) => TextStyle(
      fontFamily: fontBold,
      color: Color(color),
      fontSize: size,
    );

TextStyle textLight(double size, int color) => TextStyle(
      fontFamily: fontLight,
      color: Color(color),
      fontSize: size,
    );

TextStyle textMedium(double size, int color) => TextStyle(
      fontFamily: fontMedium,
      color: Color(color),
      fontSize: size,
    );

TextStyle textRegular(double size, int color) => TextStyle(
      fontFamily: fontRegular,
      color: Color(color),
      fontSize: size,
    );

TextStyle textSemiBold(double size, int color) => TextStyle(
      fontFamily: fontSemiBold,
      color: Color(color),
      fontSize: size,
    );

TextStyle textThin(double size, int color) => TextStyle(
      fontFamily: fontThin,
      color: Color(color),
      fontSize: size,
    );

TextStyle textUltraLight(double size, int color) => TextStyle(
      fontFamily: fontUltraLight,
      color: Color(color),
      fontSize: size,
    );
